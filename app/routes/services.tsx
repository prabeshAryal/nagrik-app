import { useMemo, useState } from "react";
import { useSearchParams } from "react-router";
import { categories, services } from "../data";
import { Icon, PageHeader, ServiceCard } from "../components";
export default function Services(){const [params,setParams]=useSearchParams();const [query,setQuery]=useState(params.get("q")||"");const active=params.get("category")||"All";const results=useMemo(()=>services.filter(s=>(active==="All"||s.category===active)&&(`${s.name} ${s.nepali} ${s.agency}`.toLowerCase().includes(query.toLowerCase()))),[active,query]);return <div className="page"><PageHeader eyebrow="NATIONAL SERVICE DIRECTORY" title="Government services" copy="Search and apply across federal, provincial, and local government from one secure account."/><div className="catalog-toolbar"><label className="catalog-search"><Icon name="search"/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search by service or office"/><span>{results.length} found</span></label><div className="category-tabs" role="tablist">{categories.map(c=><button className={active===c?"active":""} onClick={()=>{const p=new URLSearchParams(params);c==="All"?p.delete("category"):p.set("category",c);setParams(p)}} key={c}>{c}</button>)}</div></div>{results.length?<div className="service-grid catalog">{results.map(s=><ServiceCard key={s.id} service={s}/>)}</div>:<div className="empty-state"><Icon name="search" size={36}/><h2>No matching service</h2><p>Try a shorter name, an agency, or a different category.</p><button className="button secondary" onClick={()=>{setQuery("");setParams({})}}>Clear search</button></div>}</div>}
