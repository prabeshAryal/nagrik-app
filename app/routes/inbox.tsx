import { Icon, PageHeader } from "../components";
import { useApp } from "../app-context";
export default function Inbox(){const {notifications,markRead}=useApp();return <div className="page narrow-page"><PageHeader eyebrow="OFFICIAL UPDATES" title="Citizen inbox" copy="Application decisions, payment reminders, and notices from verified government offices." action={<button className="button quiet" onClick={()=>markRead()}>Mark all read</button>}/><section className="notification-list">{notifications.map(n=><button key={n.id} className={`notification ${!n.read?"unread":""}`} onClick={()=>markRead(n.id)}><span className="notification-icon"><Icon name="bell"/></span><span><b>{n.title}</b><p>{n.body}</p><small>{n.time}</small></span>{!n.read&&<i/>}</button>)}</section></div>}
